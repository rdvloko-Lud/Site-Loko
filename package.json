{
  "name": "react",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "main": "src/index.js",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "loader-utils": "3.2.1",
    "puppeteer": "^24.10.0"
  },
  "overrides": {
    "typescript": "4.9.5"
  },
  "scripts": {
    "prestart": "node scripts/generate-blog.js",
    "start": "react-scripts start",
    "blog": "node scripts/generate-blog.js",
    "build": "node scripts/generate-blog.js && node scripts/generate-sitemap.js && GENERATE_SOURCEMAP=false react-scripts build && npx puppeteer browsers install chrome && node scripts/prerender.js",
    "build:nossg": "node scripts/generate-blog.js && node scripts/generate-sitemap.js && GENERATE_SOURCEMAP=false react-scripts build",
    "prerender": "node scripts/prerender.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
